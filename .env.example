# Backend API URL (Default Spring Boot or REST backend endpoint)
VITE_API_BASE_URL=http://localhost:8080/api
# Default to true so app works out of the box with realistic mock services
VITE_USE_MOCK_API=true
